version: '3'
env:
  DOCKER_API_VERSION: "1.50"
  PACK_VOLUME_KEY: "taha-fips-cache-key"
includes:
  img: taskfile/image.yml
  builder: taskfile/builder.yml
  app: taskfile/app.yml
  util: taskfile/util.yml
  bp: taskfile/buildpack.yml


vars:
  BUILD_IMAGE: ghcr.io/taha2samy/java_fck_fips_builder:latest
  RUN_IMAGE: ghcr.io/taha2samy/java_fck_fips_running:latest
  BUILDER_NAME: ghcr.io/taha2samy/java-fips-builder:latest
  BUILDER_CONFIG: 2-builder/builder.toml

tasks:
  default:
    cmds:
      - task: all

  all:
    desc: Build everything from scratch
    cmds:
      - task: img:build-img
      - task: img:run-img
      - task: bp:package
      - task: builder:create
